import { AotwDropdownDirective } from './dropdown.directive';

describe('AotwDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new AotwDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
