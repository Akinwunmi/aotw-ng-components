import { FlagDropdownDirective } from './dropdown.directive';

describe('FlagDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new FlagDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
