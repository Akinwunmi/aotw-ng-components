export enum PillTheme {
  Dark = 'dark',
  Light = 'light',
}

export enum PillType {
  Error = 'error',
  Primary = 'primary',
  Secondary = 'secondary',
  Success = 'success',
  Warning = 'warning',
}
